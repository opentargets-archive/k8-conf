apiVersion: v1
kind: Service
metadata:
  name: api-$GIT_BRANCH
  labels:
    app: rest-api
    branch: $GIT_BRANCH
    tag: $GIT_COMMIT
    environment: staging
spec:
  ports:
  - port: 8008
    protocol: TCP
  selector:
    app: rest-api
    branch: $GIT_BRANCH
    environment: staging